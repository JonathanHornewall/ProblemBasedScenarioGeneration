# ContextualDFL

Julia workspace for contextual decision-focused learning experiments.

The core package lives in `ContextualDFL/`. Sibling packages can be added under this directory as the project grows.
