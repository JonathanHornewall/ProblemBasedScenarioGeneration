# Refactored Julia Workspace

This directory contains the new Julia implementation skeleton.

- `ContextualDFL/`: core decision-focused learning package.
- `ContextualDFLExperiments/`: experiment and benchmark package.

The files currently define package structure, public APIs, structs, abstract
types, and method signatures only. Function bodies intentionally throw
`not implemented` errors until the real logic is added.
