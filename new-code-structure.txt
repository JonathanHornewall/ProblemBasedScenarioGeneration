- Code Structure (OVER HERE NICO)
    - Package1: ContextualDFL
        - DFLScenarioGenerator.jl
            - Parametrized by learned_component
                - A symbol set. A subset of (W_ineq:, W_eq:, T_ineq:, T_eq:, h:, q:)
            - callable struct with fields NNScenarioDecoder, Solver, neural_net, program
                - NNScenarioDecorder is a ScenarioDecoder type
                - Solver is a solver type
                - neural_net is a neural nt type
                - program is a StochasticProgram type
            - When used on a context, it returns a scenario prediction
        - Directory: learning
            - DataSet.jl
                - A simple struct
                - it has four fields, x_data, \xi\_W_data, \xi\_T_data, \xi\_h_data, \xi\_q_data
                - Each is an array, indexed in colum-first typical Julia order.
            - train.jl
                - train(scenario_generator:DFLScenarioGenerator, loss:LossFunction, data_set:DataSet, scenario_decoder:DataSetScenarioDecoder, \mu_schedule, \rho_schedule, batch_size_schedule, step_size_schedule)
                - takes Loss, neural_net, \mu_schedule, \rho_schedule, step_size_schedule, batch_size_schedule
            - Directory: loss_functions
                - Loss.jl
                    - specifies an abstract class
                    - it should be callable, taking (program, $\xi$, $\tilde \xi$  , $\mu$, $\rho$)
                - DflScenLoss.jl
                    - struct subtype of loss.jl
                    - Takes scenario_realizer, solver, program
                    - straight_forward implementation
                - dfl_c_loss.jl
                    - struct subtype of loss.jl
                    - takes solver, program, \rho, \mu
                - MSE_scen_loss.jl
                    - has no fields, and is very simple
                - projected_z_loss.jl
                    - takes solver, program
            - utils/
                - test.jl
                    - test(z_policy, data_set)
                        - z_policy is a set of first-stage decisions associated with each context in the data_set
                - hyper_parameter_helpers/
                    - here we build small helper functions for creating \mu, \rho, batch_size and step_size schedules for input into training
        - Directory: scenario_decoders
            - ScenarioDecoder.jl
                - callable abstract type
                    - default behavior is to error
                - Has one concrete subtype TrivialDecoder, which has no fields. When called, it simply acts as the identity.
            - DataSetScenarioDecoder.jl
                - callable, concrete subtype of ScenarioDecoder
                    - It takes \xi_
                    - when called, it returns W_eq, W_ineq, T_eq, T_ineq, h and q.
                    - If :W_ineq is in changing_components, it returns W_ineq, otherwise base_scenario.W_ineq
                - has decoder_strategy field
                - has base_scenario field
                - parametrized by changing_components
                    - A set of symbols, containing a subset of :W_eq,:W_ineq, :T_eq, :T_ineq :h, :q
            - BaseScenario.jl
                - Implement a scenario as a struct
                - has fields W_eq, W_ineq, T_eq, T_ineq, h, q as fields, each with either with a value or with nothing
                - helper struct for ComponentWiseScenarioDecoder.jl
            - DecoderStrategy.jl
                - helper struct for ComponentWiseScenarioDecoder
                - Has fields for W_eq_decoder, W_ineq_decoder, T_eq_decoder, T_ineq_decoder, h_decoder, q_decoder. Each one is a ComponentDecoder abstract type
                - default value for each field is EmptyDecoder
            - ComponentDecoder.jl
                - Abstract callable type
                - has a component_type field, which is a symbol in the subset (W_eq, W_ineq, T_eq, T_ineq, h, q)
                - default behavior is to return an error
                - has one concrete default struct (DefaultComponentDecoder) which acts as the identity, and another default struct (EmptyComponentDecoder) which returns an error.
        - Directory: data_generation
            - directory: context_sampling/
                - ContextSampler.jl
                    - callable abstract type
                    - generate_context_set(nr_context)
                - directory: context_samplers/
            - directory: scenario_sampling
                - ScenarioSampler.jl
                    - callable abstract type which takes a context as input.
                    - generate_scenario_set(context_data, scenarios_per_context:int or vec)
                - directory: scenario_samples/
        - Directory: stochastic_programming
            - StochasticProgram
                - Has fields A_eq, A_in, b, c
            - construct_lp.jl(sp, W_eq, W_in T_eq, T_in, h_eq, h_in q_eq, q_in)
                - Figures out the extensive form of the LP, returning its lp representation as a huge matrix.
            - solve.jl
                - function implementation
                    - calls on construct_lp
                    - generates a solution
                    - partitions the solution into first-stage and second stage components. Same with the dual variables (see differentiation)
            - solve_rrule.jl
                - Here we implement the chain rules for solve.jl using the formulas in the main document
                    - First, we begin with a simple matrix construction solution.
                    - Then, once we know that works, we move to optimize via HVPs
            - cost_function.jl
                - Simply define the scenario_wise cost, and then iterate over it.
                - Probably want to return both value, and optimal primal and dual variables (see differentiation)
            - cost_function_rrule.jl
                - here we rely entirely on dual information. Very straight forward.
        - Directory: linear_programming
            - LP.jl
                - struct with field A_eq, A_in, b_eq, b_in, c_eq, c_in
            - solver.jl
                - Solver
                    - define the solver abstract type
                    - it has the method implement which is not defined, with lp and config as inputs
                    - it has a solve method which is not defined, with a type agnostic input and a config input
                    - And it has the method solve with an lp input, which first actualizes the lp and then calls a generic solve method on it.
                    - the solve method has optional arguments for A_eq, b, c, in case you want to change either one.
                - LPImplementation
                    - an abstract type, with get_implementation() method
                    - instantiate a concrete subtype with a field for a specific LP implementation, and a get_implementation method that simply returns the value of the field.
                        - An LP implementation is a representation of an LP that can be used by an actual solver.
                        - We have this as a separate object, in case we want the solver to perform in-place modifications on the code.
                - Note
                    - To implement a solver, you introduce a type for the solver, and the LP implementation, and define the solve and constructor methods for each one.
                - NOTE on implementation
                    - the first version can do away with the LPImplementation type.
                    - You should still write it out though, and comment it out. It will be very important in the next stage of the project.
                - NOTE on implementation nr 2
                    - How do we deal with the \mu=0 and \rho = 0 cases separately?
                    - We need to implement a SolverStrategy struct with one QP, LP and NLP solver as subfields
            - diff_lp.jl
                - differentiates the solve method with respect to the b and c inputs
            - directory: solvers/
                - glpk
                - gurobi
    - Package2: ContextualDFLExperiments
        - Plan
            - 
            - 
            - 
            - 
        - bechmark.jl
            - This is the main loop. It runs a training run and returns the relevant information, saving it when appropriate.
        - diagnostics.jl
            - examines value of stochasticity
        - Directory: problem_instances
            - ProblemInstance.jl
                - Abstract type with getter methods for…
                    - A
                    - b
                    - c
                    - W_base
                    - T_base
                    - h_base
                    - q_base
                - Note
                    - to set up a new problem, you will generally need to
                    - set up a problem_instance type with the appropriate  parameters 
                    - set up a context and scenario generator
                    - decide on a scenario parametrization
            - ProblemInstance
        - Ideas, etc.
            - Always benchmark with the theoretical overfitted policy
                - Check how well a policy optimal for the training data performs on the contexts in the training data
            - Always benchmark with non-stochastic solution
            - Always compare performance of policy on training data to the performance on testing data to determine overfitting tendencies
